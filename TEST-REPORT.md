# 動作確認レポート

自動ブラウザ確認（Chromium・スマホ幅390px）と構文確認を実施しました。

## 合格した項目

- JavaScript構文エラーなし（`index.html` 内スクリプト、`muze-core.js`、`muze-image.js`）
- ページ初期表示
- メンバー8名の選択肢表示
- テーマカード6件の表示
- 写真選択モーダルの表示
- 背景クリック・閉じるボタンに加え、Escキーで閉じる処理
- モーダルを閉じたあと、開いたボタンへフォーカスが戻る処理
- KairyuのJPG候補20枚の表示
- RyukiのJPG→PNGフォールバック（20枚表示）
- 4000×3000pxのアップロード画像が2048×1536pxへ縮小されること
- 6テーマの画像選択
- 1200×1200pxの完成Canvas生成
- PNG Blob生成（テスト時 約1.86MB）
- Web Share非対応時のPNGダウンロード
- 保存PNGのサイズが1200×1200pxであること
- ブラウザコンソール上の実行エラーなし

## 実機で最終確認してほしい項目

ブラウザの自動テストでは、iPhoneやSNSアプリ内ブラウザの共有シートそのものまでは再現できません。公開前に次を確認してください。

- iPhone Safariで「画像を保存する」→共有シートが開く
- 共有シートから「画像を保存」ができる
- XやInstagramのアプリ内ブラウザでの挙動
- 実際に普段使う写真を6枚アップロードしてもタブが落ちない
- 保存画像と画面プレビューのトリミングが意図どおり一致する
